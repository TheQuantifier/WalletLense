# Basic
NODE_ENV=development
PORT=5000

# MongoDB (Atlas or self-hosted)
MONGO_URI=mongodb+srv://<user>:<password>@<cluster>/<db-name>?retryWrites=true&w=majority
MONGO_DB_NAME=financeapp

# Auth
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=7d

# Frontend origin (for CORS + cookies)
CLIENT_ORIGIN=http://localhost:5500

# Uploads
UPLOAD_TMP_DIR=tmp_uploads

# OCR worker script (we’ll wire this later)
OCR_WORKER_SCRIPT=../worker/ocr_demo.py